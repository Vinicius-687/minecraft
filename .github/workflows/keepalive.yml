name: Keep Codespace Awake

on:
  schedule:
    - cron: "*/25 * * * *"  # Executa a cada 25 minutos
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep Codespace Active
        run: |
          echo "Pinging Codespace..."
          curl -I https://psychic-xylophone-gpv7jxxpg94f9pv9-8443.app.github.dev || echo "Falha ao pingar Codespace."
